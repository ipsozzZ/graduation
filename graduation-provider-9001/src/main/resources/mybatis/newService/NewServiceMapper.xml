<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="live.ipso.provider.dao.NewServiceMapper">

<update id="addLike">
        update new_service set like_num=like_num+#{number} where news_id=#{id} and del_flag=1
    </update>
</mapper>